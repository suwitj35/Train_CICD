name: Hello World
on: 
   push:
     branches: [main]
     paths-ignore:
       - ".github/workflows/*"
   pull_request:
     branches: [main]
jobs:
  hello-job :
    runs-on: ubuntu-latest
    steps:
    - name: run a one line script
      run: echo Hello World Github Actions
  Seccond-job:
    runs-on: windows-lasttest
    needs: hello-job
    steps:
    - run: echo "this Job is runing on ${{runner.os}}"
