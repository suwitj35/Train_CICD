name: Hello World
on: 
   push:
     branches: [main]
   pull_request:
     branches: [main]
jobs:
  hello-job :
    runs-on: ubuntu-latest
    steps:
    - name: run a one line script
      run: echo Hello World Github Actions
  Seccond-job:
    run-on: windows-lasttest
    need: hello-job
   stepps:
      - run: echo "this Job is runing on ${{runner.os}} "
