name: Hello World
on: 
   push:
     branches: [main]
   pull_request:
     branches: [main]
jobs:
  hello-job :
    runs-on: ubuntu-latest
    steps:
    - name: run a one line script
      run: echo Hello World Github Actions
  Seccond-job:
    run-on: windows-lasttest
   stepps:
   - run: echo "this Job is runing on "
