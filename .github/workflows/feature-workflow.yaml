name: wowrkflow for feature Branch
on:
 push: 
  branches: [feature/**]
 jobs :
   Build:
      runs-on : ubuntu-latest
      steps:
        - name: Check new feature quality
        run: echo "Sart unit testing"
